# FlyMon 핸드오프 — M1 완료, M0b/M2 시작용

작성 2026-09-14 (세션 guillemot-32). 새 세션은 이 문서 → 스펙(부록 A·B) → 다음 계획 순으로 읽는다.

## 1. 저장소 상태

- 경로 `/Users/jeonsehyeon/orca/workspaces/fruit-fly/guillemot`(orca 워크트리), 브랜치 `open-fly-brain-connectome`, HEAD `795754c (이 핸드오프 커밋 직전)`. `main`(8d50ac9)은 초기 커밋뿐. 원격 없음. 브랜치는 그대로 둔다(사용자 결정).
- 환경: Python 3.13(uv), Node 24. `uv sync && bash scripts/install_showdown.sh && uv run pytest` → 171 passed, 1 skipped, 1 xfailed(사전 등록 M0 게이트, strict), 경고 없음. 서버 미설치면 `tests/battle/test_server.py`·`test_fly_coach_player.py`는 skip.
- 결과: `results/m1/`(git 제외: 팔별 JSON, 턴 로그 JSONL, 일정), `results/summary/{m1_pilot.json, schedule_m1.json}`(커밋). M0 결과는 이전 핸드오프와 같다.
- 문서: 스펙 `docs/superpowers/specs/2026-09-14-flymon-design.md`(부록 B = M1 결과), M1 계획, `docs/pool.md`(로스터·검증표), README M1 절.

## 2. M1에서 확정된 것

- **M1 게이트 통과**: MAX − RND 승률 0.319(마리 단위 부트스트랩 95% CI [0.291, 0.344]) ≥ 0.15. 팔당 1,600배틀, 동점·미완료 0.
- 약한 코치 ≈ 강한 코치(WEAK-MAX 0.485 vs MAX 0.471). 이 풀에서 승률 차이는 공격기 선택이 만든다.
- 마리 간 분산 = 이항 하한(마리 수준 추가 분산 ≈ 0). 기준 3(승률) 검정력은 MDE 0.15에서 ≈ 1.0, 80% 검정력 MDE ≈ 0.064 — 최종 MDE·마리 수는 스펙 4.3대로 M2 종료 전에 동결.
- 모듈: `flymon/battle/{server,moves,pool,validate_pool,schedule,coach,providers,router,attribution,barrier,fly_coach_player,opponents}.py`, `scripts/{install_showdown.sh,pilot_no_brain.py}`. 로스터는 계획 제안 그대로 gen1ou 검증 통과(교체 0).
- 턴 로그 JSONL: `kind: "decision"`과 `kind: "outcome"` 레코드를 `(battle_tag, turn)`으로 잇는다(강제 교체 턴은 결정 레코드 2개 → 다대일). 승리 턴은 `|upkeep|` 없이 `|win|`으로 끝난다.
- 귀속 파서 `end_turn(expected_multiplier=None)`: M3에서 `opponent_active_pokemon.damage_multiplier(move)`를 넘기면 스펙 3.4의 상성 태그/계산 불일치 규칙이 완성된다(아직 호출부 없음).
- 배리어 계약(M3가 동결): 잔존 1명이면 즉시 실행, 진행 중 배치의 요청도 취소로 깨움, `run_batch` 길이 불일치·예외는 대기자 전원에게 전파, 같은 player_id의 중복 submit은 RuntimeError.

## 3. 판정(rulings) 위치

M1 실행 원장의 R1–R13은 세션 최종 메시지에 전문이 있다. 핵심: 로스터 교체는 마리당 공격기 2–3개 유지(R1), 귀속 파서에 동시 기절·상성 불일치 사례 추가(R2), 잔존 1명 즉시 실행(R3), 배리어 재등록은 choose_move 안에서(R4), JSONL 결정/결과 레코드 분리(R9), 동점·미완료 명시 계수(R10), 최종 리뷰 범위는 M1 구간만(R11), 중복 submit은 예외(R12).

## 4. 안 된 것 / 미해결

- 팔별 벽시계 편차 33–501 s(팔당 1,600배틀) 원인 미확인. 턴 수·잔존 서버로 설명되지 않음. M3 스모크에서 배틀별 경과 시간을 기록해 재측정(요약에 `wall_clock_s`·`mean_turns` 실림).
- 최종 리뷰에서 M3 전으로 미룬 것: JSONL 재실행 마커(스펙 3.6 체크포인트·롤백 규칙과 함께 결정), 마리→배틀 계층 부트스트랩(`analyze.py`에 구현, 현재 CI는 마리 단위라 약간 낙관적), 레코드마다 파일 열기, 배틀 태그별 dict 미정리, `secondary.boosts` 부호(하락만) 검사.
- 최종 리뷰 뒤 보류(원장 판정 포함): 파일럿 `n_flies`가 실패한 마리도 세는 것(`n_failed_flies`로 복호 가능), 배리어 중복 submit 예외가 느린 `run_batch` 중 배틀 종료 뒤 다음 배틀의 첫 submit에서 날 수 있는 좁은 경쟁(마리당 배틀은 순차라 M1에서는 도달 불가 — M3에서 지킬 것), `scripts/pilot_no_brain.py`에 테스트 모듈 없음(실패 마리 생존은 수동 검증만).

## 5. 다음 단계 순서

1. `uv sync && bash scripts/install_showdown.sh && uv run pytest` 초록 확인.
2. **M0b(MPS 스웜)**: 배치 엔진·짝지은 잡음·hive, CPU 대비 통계 일치, 처리량 → 60시간 게이트(스펙 5). 그 전에 STD(Tsodyks–Markram) 도입 여부 결정(스펙 7절, 부록 A.5).
3. **M2**: 인코더·판독·학습 단위 시험(go/no-go). 파라미터 동결·해시. MDE·마리 수 확정.
4. **M3**: 뇌 제공자를 `BatchBarrier.run_batch`에 연결, `Outcome` → PAM08/PPL105 펄스 표(스펙 3.4; REV 반전표 4.1)를 한 곳에 두기, `expected_multiplier` 호출부, 체크포인트·재개, 안전 제약 테스트 4개.

## 6. 운영상 주의

- 서브에이전트는 `results/` 아래에 쓰지 않는다. 파일럿·긴 실행은 컨트롤러가 직접 돌린다(이번 세션에서 지켜졌다).
- 파일럿 팔은 한 번에 하나씩(상대 계정 이름이 팔 사이에 겹친다). 서버는 127.0.0.1 바인딩만.
- 같은 워크트리를 다른 orca 세션이 편집할 수 있다. 시작 시 `git status`, 미커밋 변경이 있으면 `ListAgents`의 `guillemot-*`와 조율.
- 계획서 코드에도 결함이 있었다(배리어 테스트가 스펙과 모순, 다단 히트 HP, 장식 줄 기절 인접성). 구현자의 "편차" 보고는 판정하고 원장에 남긴다.
